{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install --include=dev && npx tsc"
  },
  "deploy": {
    "startCommand": "cd backend && npm start"
  }
}
